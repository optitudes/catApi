import 'package:flutter/material.dart';

class CABreedScoreCard extends StatelessWidget {
  const CABreedScoreCard(caplBreedSocre, {super.key});

  @override
  Widget build(BuildContext context) {
    return Text(this.caplBreedSocre);
  }
}
